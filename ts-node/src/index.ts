import * as HeroClasses from './classes/hero';
import powers from './data/powers';

const Hero = 123;
const spiderman = new HeroClasses.Hero('Spiderman', 30, 'Super');

console.log(spiderman);
console.log(powers);
